org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m
org.gradle.daemon=true
org.gradle.parallel=true
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Disabled: Kotlin incremental compiler's relative-path cache breaks when the
# project is accessed via a subst'd drive letter (R:) while the Pub cache
# stays on C: — different drive roots can't compute a relative path.
kotlin.incremental=false
