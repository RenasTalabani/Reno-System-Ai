apiVersion: v1
kind: ConfigMap
metadata:
  name: reno-api-config
  namespace: reno-system
data:
  NODE_ENV: "production"
  PORT: "4000"
  HOST: "0.0.0.0"
  OTEL_ENABLED: "true"
  OTEL_SERVICE_NAME: "reno-api"
